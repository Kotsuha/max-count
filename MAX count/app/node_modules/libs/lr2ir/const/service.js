"use strict";

const Enum = require("libs/enum");

const SERVICE = new Enum({
	LOGIN: "ログイン",
	ADD_RIVAL: "ライバルリストに追加",
	DELETE_RIVAL: "ライバルリストから削除"
});

module.exports = Object.freeze(SERVICE);
